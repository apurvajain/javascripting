// const promise = require("./PromiseAfterPromise");
// test("should print something when the promises are resolved", () => {
//   const consoleSpy = jest.spyOn(console, "log");
//   expect(consoleSpy).toHaveBeenCalledWith(
//     "Promise { <pending>, then: [Function] }"
//   );
// });

//first is  defined globally by promise-it-wont-hurt so cant be tested
