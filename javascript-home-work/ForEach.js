//forEach() - method calls a function once for each element in an array, in sequence.
// It dosn't return anything. it is used to iterate over array

const fruits = ["apple", "orange", "cherry"];
fruits.forEach(function(fruit){
    console.log(fruit);

})